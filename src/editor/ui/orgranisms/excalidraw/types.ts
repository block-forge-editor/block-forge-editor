export type TExcalidrawData = {
  sceneId?: string;
  imageUrl?: string;
  title?: string;
  description?: string;
};

export type TExcalidrawSceneData = {
  id: string;
  title: string;
  description?: string;
  imageUrl?: string;
};
